# Backlog

Feature ideas and known limitations that have been discussed but not implemented.
Nothing here ships until Gordon says so.

(Nothing currently pending. The "approximate address" item that used to be here
shipped in 4.4 as the "Location Lookup" line - reverse-geocodes location.latitude/
location.longitude via OpenStreetMap Nominatim, off by default, manual lookup only.)
